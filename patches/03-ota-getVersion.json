{
  "file": "src/ota.js",
  "description": "Returns the current app version instead of fetching the OTA version",
  "type": "insert_after_regex",
  "pattern": "async getVersion\\(\\)\\s*\\{",
  "insert": "\n    return app.getVersion();"
}
