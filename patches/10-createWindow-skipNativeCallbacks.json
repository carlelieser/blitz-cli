{
  "file": "src/createWindow.js",
  "description": "Skips registerNativeCallbacks to prevent its native background thread from crashing the process",
  "type": "replace",
  "find": "    registerNativeCallbacks();\n    nativeCallbacksAttached = true;",
  "replace": "    nativeCallbacksAttached = true;"
}
