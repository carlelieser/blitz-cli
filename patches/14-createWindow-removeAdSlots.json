{
  "file": "src/createWindow.js",
  "description": "Injects a MutationObserver via executeJavaScript on did-finish-load to remove ad column divs as they are added to the DOM",
  "type": "insert_after_regex",
  "pattern": "browserView\\.webContents\\.loadURL\\(url\\);",
  "insert": "\n  browserView.webContents.on(\"did-finish-load\", () => {\n    browserView.webContents.executeJavaScript(\n      \"(function() { function removeAncestor(el) { if (!el.isConnected) return; while (el.parentElement && el.parentElement.childElementCount === 1) el = el.parentElement; el.remove(); } function clean() { document.querySelectorAll('[class*=\\\"\\\\uD83E\\\\uDD11-column\\\"]').forEach(function(el) { if (el.isConnected) el.remove(); }); document.querySelectorAll('img[src^=\\\"https://blitz-cdn.blitz.gg/self-promotion\\\"]').forEach(function(el) { removeAncestor(el); }); document.querySelectorAll('[class*=\\\"\\\\uD83E\\\\uDD11-container\\\"], [class*=\\\"display-\\\\uD83E\\\\uDD11\\\"]').forEach(function(el) { removeAncestor(el); }); document.querySelectorAll('.route-wrapper').forEach(function(el) { el.style.removeProperty('width'); }); } clean(); var _t; new MutationObserver(function() { clearTimeout(_t); _t = setTimeout(clean, 50); }).observe(document.documentElement, { childList: true, subtree: true }); }());\"\n    ).catch(function() {});\n  });"
}
